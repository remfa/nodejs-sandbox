const { sum, mult} = require('./app07-helper2');

console.log("essai method auto logger with proxy and apply handler");

console.log("sum(5, 4)=" + sum(5, 4));

console.log("mult(8,3)=" + mult(8,3));
